window.onload = start;

function start() {

    // webpod options
    const options = [
        {
            theme: dark
        },

        {
            position: bottom-right
        }
    ];

    // webpod constructor

    // new webpod
    const webpod = new Webpod(options);
}